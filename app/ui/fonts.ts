import { Inter, Lusitana, Merriweather } from 'next/font/google';

export const inter = Inter({
    subsets: ['latin']
});
export const lusitana = Lusitana({
    weight: ["400", "700"],
    subsets: ['latin'],
});

export const secondFont = Merriweather({
    weight: ["400", "700"],
    subsets: ['latin'],
});